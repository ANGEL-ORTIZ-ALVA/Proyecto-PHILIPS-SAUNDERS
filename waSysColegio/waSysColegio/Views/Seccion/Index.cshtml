@model IEnumerable<waSysColegio.Seccion>

@{
    ViewBag.Title = "Index";
}
@Html.Partial("_Head_Content")
@Html.Partial("_Sidebar")

<section class="home">
    <div class="text">
        <figure>
            <img src="../Resources/logo-school.png" width="50" height="50" />
        </figure>
        <h2>Colegio 'Philip P. Saunders'</h2>
    </div>
    <div class="text">
        <div class="form-container">
            <div style="margin-bottom:10px;">
                <h3 class="form-title">Lista de Secciones</h3>
                @Html.ActionLink("Nueva Seccion", "Create", null, new { @class = "btn btn-success" })
            </div>

            <div class="table-responsive">
                <table class="table">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Nombre_Seccion)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Aforo)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Estado_Registro)
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nombre_Seccion)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Aforo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Estado_Registro)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.ID_Seccion }, new { @class = "editar", style = "margin-right: 5px;" })
                                @Html.ActionLink("Details", "Details", new { id = item.ID_Seccion }, new { @class = "info", style = "margin-right: 5px;" })
                                @Html.ActionLink("Delete", "Delete", new { id = item.ID_Seccion }, new { @class = "borrar" })
                            </td>
                        </tr>
                    }

                </table>
            </div>
        </div>
    </div>
</section>
<style>
    .btn {
        text-decoration: none;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: 0.3s;
    }

    .btn-primary {
        background-color: #4CAF50;
        color: #fff;
    }

        .btn-primary:hover {
            background-color: #3e8e41;
        }

    .btn-info {
        background-color: #03A9F4;
        color: #fff;
    }

        .btn-info:hover {
            background-color: #0397d6;
        }

    .btn-danger {
        background-color: #F44336;
        color: #fff;
    }

        .btn-danger:hover {
            background-color: #cb362c;
        }
</style>
@Html.Partial("_Footer")

