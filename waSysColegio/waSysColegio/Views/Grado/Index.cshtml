@model IEnumerable<waSysColegio.Grado>

@{
    ViewBag.Title = "Index";
}
@Html.Partial("_Head_Content")
@Html.Partial("_Sidebar")

<section class="home">
    <div class="text">
        <figure>
            <img src="../Resources/logo-school.png" width="50" height="50" />
        </figure>
        <h2>Colegio 'Philip P. Saunders'</h2>
    </div>
    <div class="text">
        <div class="form-container">
            <h4 class="form-title">Lista de grados</h4>
            <div style="margin-bottom: 10px;">
                @Html.ActionLink("Registrar grado", "Create", null, new { @class = "btn btn-success" })
            </div>
            <div class="table-responsive">
                <table class="table">
                    <tr>
                        <th>
                            Número de grado
                        </th>
                        <th>
                            Estado de registro
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Numero_Grado)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Estado_Registro)
                            </td>
                            <td>
                                <div class="btn-group">
                                    @Html.ActionLink("Editar", "Edit", new { id = item.ID_Grado }, new { @class = "editar", style = "margin-right: 5px;" }) 
                                    @Html.ActionLink("Detalles", "Details", new { id = item.ID_Grado }, new { @class = "info", style = "margin-right: 5px;" }) 
                                    @Html.ActionLink("Eliminar", "Delete", new { id = item.ID_Grado }, new { @class = "borrar" })
                                </div>
                            </td>
                        </tr>
                    }

                </table>
            </div>
        </div>
    </div>
</section>
<style>
    .btn {
        text-decoration: none;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: 0.3s;
    }

    .btn-primary {
        background-color: #4CAF50;
        color: #fff;
    }

        .btn-primary:hover {
            background-color: #3e8e41;
        }

    .btn-info {
        background-color: #03A9F4;
        color: #fff;
    }

        .btn-info:hover {
            background-color: #0397d6;
        }

    .btn-danger {
        background-color: #F44336;
        color: #fff;
    }

        .btn-danger:hover {
            background-color: #cb362c;
        }
</style>
@Html.Partial("_Footer")
